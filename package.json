{
  "private": true,
  "name": "hello-world-again",
  "homepage": "https://hello-world-again-mdeway.netlify.com",
  "scripts": {
    "deploy": "netlify deploy --prod --dir=public",
    "postdeploy": "echo 'Your site was deployed to https://hello-world-again-mdeway.netlify.com'",
    "start": "browser-sync start --server 'public' --files 'public'"
  },
  "devDependencies": {
    "browser-sync": "^2.27.10",
    "stylelint": "^9.10.1",
    "stylelint-config-standard": "^18.2.0"
  },
  "dependencies": {
    "gulp": "^4.0.2"
  }
}
